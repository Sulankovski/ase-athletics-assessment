/** Pass as the second arg to `<Link state={...}>` / `navigate(..., { state })` for player profile back link. */
export const PLAYER_NAV_FROM_DASHBOARD = { from: 'dashboard' };
export const PLAYER_NAV_FROM_PLAYERS_LIST = { from: 'players' };
